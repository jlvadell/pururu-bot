[tox]
envlist = py312
skipsdist = True

[testenv]
deps =
    pytest
    pytest-cov
    discord.py
    python-dotenv
    google-auth
    gspread
commands = pytest --cov=. --cov-report=xml --cov-config=tox.ini --cov-branch